name: Test Trigger

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "GitHub Actions is working!"